## 1. MeinTeamPage — Zurück-Button

- [x] 1.1 `useNavigate` aus `react-router-dom` importieren und Hook im MeinTeamPage-Komponenten einbinden
- [x] 1.2 `ChevronLeft` aus `lucide-react` importieren
- [x] 1.3 Zurück-Button direkt vor `<h1>` rendern — nur wenn `focusTeamId != null`; Stil und `navigate(-1)`-Handler analog zu TermineDetailPage
