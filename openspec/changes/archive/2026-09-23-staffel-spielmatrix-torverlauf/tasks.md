## 1. Aggregat im Store

- [x] 1.1 `PlayerGameMatrix(ctx, staffelID, userID)` in `internal/gamestats/aggregates.go`: löst über das vorhandene `Affiliation` die eigenen Mannschaftsnamen auf und liefert `[]TeamMatrix` (je Mannschaft: Spiele, Spieler, Zellen, Summen). Keine Mannschaft ⇒ leere Liste, kein Fehler. Verifikation: Test mit Spieler-, Trainer- und Elternteil-Zugang liefert die Mannschaft; Test ohne verknüpfte Begegnung liefert `teams: []` trotz ähnlichen Namens (design.md §2).
- [x] 1.2 Spielliste je Mannschaft: alle `bwhv_games` der Staffel mit `home_goals IS NOT NULL`, in denen die Mannschaft Heim oder Gast ist, nach Datum sortiert; je Spiel `bwhvGameId`, `date` (auf `date[:10]` getrimmt — DATE-Gotcha), `homeTeam`, `guestTeam`, `isHome`, `homeGoals`, `guestGoals`, `hasReport` (Bericht im Zustand `parsed`). Verifikation: Test mit einem gespielten Spiel ohne Bericht, einem mit Bericht und einem noch nicht gespielten belegt zwei Spalten und die richtigen `hasReport`-Werte (design.md §3).
- [x] 1.3 Zellen je Spieler und Spiel aus `bwhv_player_games`, gebunden über `teamNameExpr` an den Spielplan-Namen, gefiltert auf `r.state='parsed'`, mit `twoMinCounted`/`redCounted` statt der Rohspalten; keine Zeile ⇒ **keine** Zelle (`null`), nicht `0`. Verifikation: Test belegt `null` für einen Spieler ohne Mannschaftslistenzeile und `0` für einen mit Zeile ohne Tor (design.md §4); Test belegt `2× 2min + 1× Rot` bei drei Zeitstrafen (design.md §5).
- [x] 1.4 Je Mannschaft `halfDurationMinutes` aus `age_class_game_rules` über `teams.age_class` der zugehörigen `teams.id` mitliefern (dieselbe Auflösung wie `internal/games/regen.go:1339`); ohne gepflegte Regel bleibt das Feld `null` — **kein** Standardwert. Verifikation: Test mit gepflegter Regel liefert die Zahl, Test ohne Regel liefert `null` (design.md §7).
- [x] 1.5 Summen: je Spieler über alle Spiele plus `games` (Zahl der Berichte mit Mannschaftslistenzeile), je Spiel über alle Spieler der Mannschaft, dazu `gamesTotal`/`reportGames` je Mannschaft. Spielerreihenfolge alphabetisch. Verifikation: Test prüft, dass die Spielsumme der Spalte dem in `bwhv_games` erfassten eigenen Endstand entspricht, wenn ein Bericht vorliegt.
- [x] 1.6 Zwei eigene Mannschaften derselben Staffel ergeben zwei Einträge. Verifikation: Test mit zwei verknüpften Spielen zweier eigener Mannschaften belegt zwei getrennte Matrizen (design.md §2).

## 2. Route und Handler

- [x] 2.1 `GetPlayerGames` in `internal/gamestats/handler.go` — `staffelOfRequest` für die 404-Behandlung, `claims.UserID` für die Zugehörigkeit, Fehler über `httpx.WriteError`. Verifikation: `go build ./...` und `golangci-lint run` grün.
- [x] 2.2 `r.Get("/api/staffeln/{id}/player-games", h.GameStats.GetPlayerGames)` im Authenticated-Tier von `internal/app/router.go`, neben `/affiliation` (englisch nach der Namenskonvention). Verifikation: Broadcast-Gate bleibt grün — Lese-Route, kein Allowlist-Eintrag nötig.
- [x] 2.3 Happy-Path und Fehlerfälle testen: 200 mit Matrix, 200 mit leerer Mannschaftsmenge, 404 bei unbekannter Staffel, 401 ohne Token. Verifikation: `go test ./internal/gamestats/` grün.
- [x] 2.4 Die neue `{id}`-Route in `openByDesign` der Objektrechte-Matrix eintragen, mit derselben Begründung wie `/tabelle` und `/affiliation`. Verifikation: `go test ./internal/permissions/` grün.

## 3. Ableitung des Torverlaufs (Frontend-Logik)

- [x] 3.1 `web/src/lib/torverlauf.ts`: `torereihe(events)` filtert `goal`/`seven_m_goal` mit gesetzter Seite, zählt den Spielstand mit und liefert je Tor `{ seq, gameSecond, side, scorer, sevenMeter, scoreHome, scoreGuest, lauf, situation }`. Verifikation: Vitest belegt Laufzählung (1,2,3 und Rücksetzung) und Situation aus Sicht des Schützen.
- [x] 3.2 `halbzeitGrenze(tore, homeGoalsHt, guestGoalsHt)`: Index des Tores, mit dem der Halbzeitstand erstmals erreicht ist; `null`, wenn kein Halbzeitstand vorliegt. Verifikation: Vitest belegt die Trennung am Halbzeitstand und die durchgehende Achse ohne ihn (design.md §6).
- [x] 3.3 `halbzeitMinuten(tore, grenze, konfiguriert)` nach design.md §7: konfigurierte Dauer übernehmen, wenn `letzteSekundeHZ1 <= konfiguriert*60` **und** `ersteSekundeHZ2 >= konfiguriert*60`; sonst aus dem Verlauf ableiten. Verifikation: Vitest deckt vier Fälle ab — passende Regel, zu lange Regel (30 bei der 2×25-Begegnung der Fixture `905272`), zu kurze Regel, keine Regel.
- [x] 3.4 `achsen(tore, grenze, halbzeitMinuten)`: Rückgabe je Halbzeit `{ vonMinute, bisMinute, tore }`. Verifikation: Vitest prüft die Invariante „kein Tor liegt jenseits von `bisMinute` oder vor `vonMinute`" über alle vier Fälle aus 3.3 und belegt, dass dieselbe Torreihe mit drei verschiedenen Spieldauern dieselbe Halbzeitaufteilung ergibt (design.md §6).
- [x] 3.5 Auszeiten, Strafen und verworfene Siebenmeter erzeugen keinen Eintrag. Verifikation: Vitest mit einem Verlauf, der alle vier Ereignisarten enthält.

## 4. Darstellung

- [x] 4.1 `web/src/components/staffeln/TorMomentum.tsx`: Inline-SVG je Halbzeit, zwei waagerechte Achsen (Heim oben, Gast unten), Minutenbeschriftung im Fünferraster, Kreis je Tor mit Radius `4 + lauf * 1.5` und Farbe nach Situation aus den brand-Tokens (`#3E4A98`/`#9CA3AF`/`#F59E0B`), `<title>` mit Minute, Schütze, Spielstand und Siebenmeter-Vermerk, Legende darunter. Kein Canvas, keine neue Abhängigkeit. Verifikation: Vitest belegt, dass jeder Kreis ein `<title>` trägt (design.md §9).
- [x] 4.2 Dialog um `TorMomentum` (Kopf: Paarung und Endstand, `<X>` aus `lucide-react`, Schließen per Escape und Klick auf den Hintergrund), Modal-Klassenstring aus `docs/agent/05-frontend.md`. Verifikation: `pnpm -C web lint` grün, Dialog-Test auf Escape.
- [x] 4.3 `web/src/components/staffeln/Spielmatrix.tsx`: Tabelle mit fixierter Spielerspalte (`sticky left-0`), Spaltengruppe je Spiel (Mobile nur „Tore", ab `sm:` alle sechs Werte), Kopf mit Datum, Paarung, Endstand und „Ablauf"-Knopf bei `hasReport`, Summenspalte rechts, Summenzeile unten, Fußzeile mit „N Spiele, davon M mit Bericht". `–` für fehlende Zelle, `0` für null Tore. Verifikation: Vitest belegt die Unterscheidung `–`/`0`, den fehlenden Knopf ohne Bericht und die beiden Spielzahlen.
- [x] 4.4 Typen `TeamMatrix` (inkl. `halfDurationMinutes: number | null`), `MatrixGame`, `MatrixPlayer`, `MatrixCell` und `fetchPlayerGames(id)` in `web/src/lib/staffeln.ts` ergänzen. Verifikation: `pnpm -C web build` grün.
- [x] 4.5 Reiter „Verlauf" in `web/src/pages/StaffelnPage.tsx`: `StandingsChart` wie bisher, darunter je Eintrag aus `teams` eine `Spielmatrix`; leere Menge ⇒ Hinweis („Für diese Staffel ist noch keine eigene Begegnung verknüpft …"). Abruf in das bestehende `Promise.all` von `reload()` aufnehmen, damit `bwhv-updated` die Matrix mitzieht. Verifikation: SSE-Ereignis lädt die Matrix nach (manuell oder Komponententest mit gemocktem Hook).

## 5. Abschluss

- [x] 5.1 `make test` und `pnpm -C web test` grün, `make lint` ohne neue Befunde. Verifikation: beide Läufe lokal.
- [x] 5.2 `/verify-change` durchlaufen (Route→Tests, brand-Tokens, lucide-Icons, keine Migration, `openspec validate`). Verifikation: Checkliste ohne offene Punkte.
- [x] 5.3 Gotcha-Absatz „Staffel-Statistiken" in `docs/agent/06-gotchas.md` um die beiden nicht-ableitbaren Punkte ergänzen: Halbzeitgrenze aus dem Kopf-Halbzeitstand statt aus der Spielzeit (§6) und die Prüfung der konfigurierten Spieldauer gegen den Bericht (§7), inklusive des Belegs aus der Fixture `905272` (`mB-RL-BW` mit 25-Minuten-Halbzeiten). Verifikation: der Absatz nennt beide Regeln samt Begründung.
